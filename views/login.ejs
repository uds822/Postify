<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
  <div class="w-full text-white min-h-screen bg-gradient-to-br from-blue-800 to-purple-600 flex items-center justify-center p-6">
    <div class="bg-gray-800 p-8 rounded-lg shadow-lg w-full max-w-md">
      <h3 class="text-3xl mb-5 text-center">🔐 Login Account</h3>

      <!-- Error message display -->
     

      <form action="/login" method="post">
        <div class="mb-4">
          <input 
            class="w-full px-3 py-2 rounded-md outline-none bg-transparent border-2 border-gray-600 focus:border-blue-400 transition-colors duration-200" 
            type="email" 
            name="email" 
            placeholder="📧 Email" 
            required
          >
        </div>
        <div class="mb-4">
          <input 
            class="w-full px-3 py-2 rounded-md outline-none bg-transparent border-2 border-gray-600 focus:border-blue-400 transition-colors duration-200" 
            type="password" 
            name="password" 
            placeholder="🔒 Password" 
            required
          >
        </div>
        <div class="text-center">
          <input 
            class="px-5 py-2 rounded-md bg-blue-500 hover:bg-blue-600 text-white cursor-pointer transition-colors duration-200" 
            type="submit" 
            value="🔑 Login Account"
          >
        </div>
      </form>

      <p class="mt-4 text-center text-gray-300">Don't have an account? <a href="/" class="text-blue-400 hover:underline">📝 Register here</a></p>
    </div>
  </div>
</body>
</html>
