<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Post ✏️</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-r from-purple-600 via-pink-500 to-red-500 text-white min-h-screen flex items-center justify-center">
  <div class="w-full max-w-lg p-8 bg-white rounded-lg shadow-2xl text-zinc-800">
    <div class="flex justify-between items-center mb-6">
      <h2 class="text-2xl font-bold flex items-center">
        ✏️ Edit Post
      </h2>
      <a class="bg-red-500 hover:bg-red-600 text-white rounded-full px-4 py-2 text-sm flex items-center" href="/logout">
        🚪 Logout
      </a>
    </div>
    
    <form action="/update/<%= post._id %>" method="post" class="space-y-6">
      <div>
        <label for="content" class="block text-lg font-semibold mb-2">💬 What's on your mind?</label>
        <textarea id="content" name="content" rows="5" class="w-full p-4 bg-zinc-100 border border-zinc-300 rounded-lg resize-none focus:ring-pink-500 focus:border-pink-500 placeholder-gray-500" placeholder="Write your thoughts here..."><%= post.content %></textarea>
      </div>
      
      <div class="flex justify-between items-center">
        <input class="px-5 py-2 bg-gradient-to-r from-yellow-400 to-yellow-500 text-black font-semibold rounded-md cursor-pointer hover:from-yellow-500 hover:to-yellow-600 flex items-center" type="submit" value="✅ Update Post">
        <a href="/profile" class="text-sm text-gray-500 hover:text-pink-500 flex items-center">❌ Cancel</a>
      </div>
    </form>
  </div>
</body>
</html>
