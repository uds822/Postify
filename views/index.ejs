<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Create Account</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    body {
      overflow: hidden; /* Prevent scroll bars */
    }
    .star {
      position: absolute;
      background: rgba(255, 255, 255, 0.8);
      border-radius: 50%;
      animation: twinkle 1.5s infinite alternate;
    }
    
    @keyframes twinkle {
      0% {
        transform: scale(1);
        opacity: 0.5;
      }
      100% {
        transform: scale(1.5);
        opacity: 1;
      }
    }
    
    .sparkle {
      position: absolute;
      background: rgba(255, 255, 255, 1);
      border-radius: 50%;
      animation: sparkle 2s infinite;
    }
    
    @keyframes sparkle {
      0% {
        transform: scale(1);
        opacity: 1;
      }
      50% {
        transform: scale(1.2);
        opacity: 0.5;
      }
      100% {
        transform: scale(1);
        opacity: 1;
      }
    }

    .bubble {
      position: absolute;
      background: rgba(255, 255, 255, 0.3);
      border-radius: 50%;
      opacity: 0.5;
      animation: rise 4s infinite;
      pointer-events: none; /* Prevent interaction */
    }

    @keyframes rise {
      0% {
        transform: translateY(100vh);
        opacity: 0.7;
      }
      100% {
        transform: translateY(-100px);
        opacity: 0;
      }
    }

    .boom {
      animation: boom 0.5s forwards;
    }

    @keyframes boom {
      0% {
        transform: scale(1);
        opacity: 1;
      }
      50% {
        transform: scale(1.2);
        opacity: 0.8;
      }
      100% {
        transform: scale(1);
        opacity: 1;
      }
    }
  </style>
</head>
<body>
  <div class="w-full text-white min-h-screen bg-gradient-to-br from-blue-900 to-purple-900 p-11 flex flex-col items-center relative">
    <h3 class="text-4xl mb-5">🌟 Create Account 🌟</h3>
    <form action="/register" method="post" class="bg-zinc-800 p-8 rounded-lg shadow-lg w-full max-w-md" onsubmit="return boomEffect()">
      <input class="w-full mb-4 px-3 py-2 rounded-md outline-none bg-transparent border-2 border-zinc-600 focus:border-blue-400 transition-colors duration-200" type="text" name="name" placeholder="🧑 Name" required>
      <input class="w-full mb-4 px-3 py-2 rounded-md outline-none bg-transparent border-2 border-zinc-600 focus:border-blue-400 transition-colors duration-200" type="text" name="username" placeholder="👤 Username" required>
      <input class="w-full mb-4 px-3 py-2 rounded-md outline-none bg-transparent border-2 border-zinc-600 focus:border-blue-400 transition-colors duration-200" type="password" name="password" placeholder="🔒 Password" required>
      <input class="w-full mb-4 px-3 py-2 rounded-md outline-none bg-transparent border-2 border-zinc-600 focus:border-blue-400 transition-colors duration-200" type="email" name="email" placeholder="📧 Email" required>
      <input class="w-full mb-4 px-3 py-2 rounded-md outline-none bg-transparent border-2 border-zinc-600 focus:border-blue-400 transition-colors duration-200" type="number" name="age" placeholder="🎂 Age" required>
      <input id="submit-btn" class="w-full px-5 py-2 rounded-md bg-blue-600 hover:bg-blue-500 text-white cursor-pointer transition-colors duration-200" type="submit" value="🌈 Create Account">
    </form>
    <p class="mt-4 text-center">
      Already registered? <a href="/login" class="text-blue-400 hover:underline">Login here</a> 🔑
    </p>
    
    <!-- Star and bubble background -->
    <div class="absolute inset-0 pointer-events-none">
      <!-- Generate stars -->
      <script>
        const numberOfStars = 100;
        for (let i = 0; i < numberOfStars; i++) {
          const star = document.createElement('div');
          star.classList.add('star');
          star.style.width = Math.random() * 3 + 'px'; // Random width
          star.style.height = star.style.width; // Make it a circle
          star.style.top = Math.random() * 100 + 'vh'; // Random vertical position
          star.style.left = Math.random() * 100 + 'vw'; // Random horizontal position
          star.style.animationDuration = Math.random() * 1 + 1 + 's'; // Random duration
          document.body.appendChild(star);
        }

        // Generate sparkles
        for (let i = 0; i < numberOfStars / 2; i++) {
          const sparkle = document.createElement('div');
          sparkle.classList.add('sparkle');
          sparkle.style.width = Math.random() * 5 + 'px'; // Random width
          sparkle.style.height = sparkle.style.width; // Make it a circle
          sparkle.style.top = Math.random() * 100 + 'vh'; // Random vertical position
          sparkle.style.left = Math.random() * 100 + 'vw'; // Random horizontal position
          sparkle.style.animationDuration = Math.random() * 2 + 1 + 's'; // Random duration
          document.body.appendChild(sparkle);
        }

        // Generate bubbles
        const numberOfBubbles = 10; // Adjust the number of bubbles
        for (let i = 0; i < numberOfBubbles; i++) {
          const bubble = document.createElement('div');
          bubble.classList.add('bubble');
          const size = Math.random() * 50 + 30; // Random size between 30 and 80
          bubble.style.width = size + 'px';
          bubble.style.height = size + 'px';
          bubble.style.left = Math.random() * 100 + 'vw'; // Random horizontal position
          bubble.style.animationDuration = Math.random() * 3 + 3 + 's'; // Random rise duration
          document.body.appendChild(bubble);
        }

        // Boom effect function
        function boomEffect() {
          const button = document.getElementById('submit-btn');
          button.classList.add('boom');
          setTimeout(() => {
            button.classList.remove('boom');
          }, 500); // Remove the boom class after the animation duration
          return true; // Continue form submission
        }
      </script>
    </div>
  </div>
</body>
</html>
